# Sleepship Configuration

# Aliases for commonly used commands
[aliases]
dev = "sync tasks-dev.txt"
test = "sync tasks-test.txt --max-retries=5"
prod = "sync tasks-prod.txt --max-retries=10"
integration = "sync tasks-integration-test.txt"
quick = "sync tasks-integration-test.txt --max-retries=1"
